name: main

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      
      - name: Build and push
        shell: bash
        run: |
          docker buildx build --platform=linux/arm64 .
